# RagaDetector — Project Plan

Last updated: 2026-07-08

This file is meant to be the anchor document for this project — read it at the start of any Claude Code session (alongside `CLAUDE.md`) before making changes, and update it as milestones are hit or the plan changes.

## 1. What this project is

A CPU-only Hindustani classical Raaga identifier. It listens to live microphone audio or a dropped-in music file (Phase 5, 2026-07-08), extracts pitch, builds a tonic-relative 120-bin pitch-class histogram, and compares it against reference Raaga templates via cosine similarity to guess which Raaga is being performed.

## 2. Where things actually stand today

~~Two working prototype entry points exist~~ **As of Phase 4 (2026-07-08), one entry point:**

- `main_22.py` — a 7-raga "studio" mode (Yaman, Bhupali, Bhairav, Asavari, Jaunpuri, Marwa, Puriya) where you tell it which raga you intend to sing, and it shows a live PASS/FAIL comparison against that target.
- `main.py` (the old 2-raga Yaman/Bhupali mode with tie-breaking/lock logic) was retired in Phase 4 — its registry was a strict subset of `main_22.py`'s, and its one distinguishing feature (the lock hack) was ablation-tested and removed in Phase 3 for measuring zero benefit.

It runs for a fixed 120-second window and prints a JSON summary at the end. This is useful for testing, but it's a test-harness shape, not yet a "just detect whatever raga I'm singing" tool.

The honest problems, found while reviewing the code just now:

- ~~**Three separate, drifted Raaga databases.**~~ **Resolved in Phase 1 (2026-07-08).** `config.py::RAAGA_DATABASE` (the richest schema — aroha, avroha, pakad, vadi, samvadi, prahar) now covers all 7 ragas and is the single source both `main.py` and `main_22.py` import from; the inline registries and idealized-histogram generators were deleted from both.
- ~~**Hand-tuned, per-raga hacks instead of a general classifier.**~~ **Resolved in Phase 3 (2026-07-08).** `main.py`'s Yaman similarity bonus, its 90-second Bhupali lock, and `main_22.py`'s flat forbidden-note penalty were all ablation-tested against the eval harness and removed (each measured zero accuracy benefit once Phase 2's weighted histograms were in place). Both entry points now run plain cosine similarity with no hand-tuned bonuses/penalties/locks.
- ~~**Tonic (Sa) is entered by hand every run.**~~ **Resolved in Phase 5 (2026-07-08).** `FeatureExtractor.auto_detect_tonic()` is now wired up — leaving the tonic prompt blank triggers auto-detection instead of requiring a typed Hz value. Caveat: its precision is limited (~30 cents off on a clean synthetic test), which can occasionally push a borderline case to a different raga; a manually-entered tonic is still more reliable when accuracy matters.
- **No objective evaluation.** There's no labeled dataset or test set to score classifier accuracy against. Progress so far has likely been judged by singing live and eyeballing the dashboard, which makes it very hard to tell whether a change actually helped or just fixed the one case you were staring at (this is almost certainly why work with Devin/Gemini started "spinning wheels" — every fix was a one-off reaction, with nothing to check it against, so old cases could quietly break while a new one got patched).
- **`instructions.md` is aspirational, not a status report.** It describes HMMs, SVMs, ensemble classifiers, tala detection, TensorFlow Lite, a plugin architecture — none of which exist in the code. It's useful as a reference for future direction, but it doesn't reflect what's built.
- **Housekeeping debt.** `raga_detector.log` had grown to 265KB and was being tracked by git before we added `.gitignore`; `__pycache__` likewise. ~~Two entry points with overlapping but diverging logic is itself a maintenance cost.~~ Resolved in Phase 4 — down to one entry point.

None of this means the project is in bad shape — the core pipeline (pitch detection → cents conversion → histogram → cosine similarity) is a legitimate, well-understood approach for this problem, and it's already running end-to-end on live audio. The issue is architectural drift and a lack of a feedback loop to measure progress, not a fundamentally broken idea.

## 3. Why progress stalled

Most likely pattern: each AI coding session (Devin, Gemini) was handed a live symptom ("it keeps guessing Bhupali," "Yaman never gets picked") and asked to fix it, with no persistent ground-truth test set and no single source of truth for the raga definitions. Each tool fixed the symptom in front of it by adding a special case to whichever file it happened to be looking at, which is how you end up with three databases and several hardcoded bonuses/penalties. Without an eval set, there was no way to tell if a "fix" was a real improvement or an overfit to one test.

## 4. Recommended approach: fix the process, then the code

The methodical fix is to stop patching symptoms and put a few structural things in place first, in this order. Each phase is small enough to be one or two focused Claude Code sessions.

### Phase 1 — Consolidate to one source of truth
Pick a single Raaga database schema (recommend `config.py::RAAGA_DATABASE`'s richer schema — aroha, avroha, pakad, vadi, samvadi, forbidden/required notes — since it's the most complete) and make both entry points import from it. Delete the inline registries in `main.py` and `main_22.py`. Expand it to cover the 7 ragas `main_22.py` currently has inline, at minimum.

### Phase 2 — Build a real evaluation harness
Before touching classifier logic again, create a small labeled test set — even a handful of short recordings (or synthetic tone sequences generated from each raga's aroha/avroha) with known correct answers — and a script that runs the classifier against all of them and reports accuracy. This turns "does this help?" from a guess into a number you can compare run to run. This is the single highest-leverage step to stop the spinning-wheels pattern.

### Phase 3 — Replace ad hoc hacks with justified logic
With the eval harness in place, revisit the hardcoded bonuses/penalties/lock heuristics. Either remove them and see how much accuracy the plain cosine-similarity approach gets on its own, or keep only the ones that measurably improve the eval score — and document why each one exists.

**Two-stage classification (Arun's addition, 2026-07-08):** Turn "forbidden notes" from a soft similarity penalty into a first-pass elimination filter — if a raga's forbidden swara shows sustained energy in the live histogram above a threshold, drop that raga from the candidate set entirely before running cosine similarity on whatever remains. This is musicologically sound (several ragas are defined partly by which notes they exclude, e.g. Bhupali has no Ma/Ni) and should narrow the field faster than a penalty multiplier does. Caveat to test against the eval harness: the elimination threshold needs to tolerate pitch-detection noise (a single stray frame misreading a forbidden note shouldn't disqualify the correct raga) — likely wants the same kind of sustained/energy-over-time threshold the current 4% penalty check uses, not a one-frame trigger.

**Known histogram-identical pairs — honest-tie reporting (Arun's addition, 2026-07-08):** Measured directly against the Phase-1-consolidated `config.py` database: Asavari vs Jaunpuri and Marwa vs Puriya both score cosine similarity 1.0000 against each other — completely indistinguishable by the current flat-histogram method, because they share the same note set and differ only in aroha shape (Asavari's aroha skips Ga_komal on the way up; Jaunpuri's doesn't), vadi/samvadi emphasis (Marwa: Re_komal/Dha_shuddha vs Puriya: Ni_shuddha/Ga_shuddha, with Puriya characteristically starting from the lower Ni), and pakad — none of which the classifier currently looks at. Until Phase 8's sequence/pakad matching exists to actually resolve these, the classifier should detect when top candidates' reference histograms are near-identical (cosine similarity above ~0.98) and report them jointly, e.g. "Asavari or Jaunpuri — indistinguishable by current method," rather than silently guessing one and being wrong roughly half the time. Phase 8 is the real fix; this is the honest interim behavior until then.

### Phase 4 — Retire `main.py`, keep one entry point
Arun's read (and it holds up): `main.py`'s 2-raga registry (Yaman, Bhupali) is a strict subset of `main_22.py`'s 7-raga registry, and `main_22.py` is the more recently edited file. `main.py`'s only unique content is its tie-break/lock hack, which Phase 3 should be evaluating (and likely discarding or replacing) anyway. Plan: once Phase 1–3 are done and `main_22.py` is confirmed stable, delete `main.py` rather than merging it — there's nothing in it worth preserving beyond what the eval harness already tells you about the lock heuristic.

### Phase 5 — Quality-of-life
Wire up `auto_detect_tonic()` so tonic doesn't need to be typed in by hand each run. Add support for dropping in a music file (not just live mic) as an input source, since that's part of the original goal.

### Phase 6 — Only now, consider the aspirational features
Things like pakad (phrase) matching, HMMs, ensemble classifiers, tala detection from `instructions.md` are legitimate v2 ideas, but only worth adding once there's a reliable way (the eval harness) to tell whether they actually improve results.

### Phase 7 — Practice/learning mode (Arun's addition, 2026-07-08)
Extend the existing "studio" comparison (already in `main_22.py`: pick a raga, sing, see live vs. target) into an actual learning tool that points out specific variations rather than just a coarse bar chart and PASS/FAIL. Depends on Phase 1 (needs the richer `config.py` schema — aroha/avroha/pakad, not just a flat histogram) and ideally Phase 2's eval harness for validating that the feedback given is musically correct. Not a separate project — it reuses the entire audio/pitch/histogram/database pipeline; only the "what do we do with the comparison" step differs from guess-the-raga mode. Candidate feedback types: per-swara sharp/flat deviation in cents (needs finer resolution than the current 12-bucket semitone sum), accidental forbidden-note hits, aroha/avroha sequence adherence.

### Sequencing note: expand raga coverage before Phase 8 (Arun's addition, 2026-07-08)
Decision: grow the raga database well beyond the current 7 before starting Phase 8's sequence/pakad work, rather than building sequence-matching machinery around a small set and re-doing it later. While expanding, explicitly catalog same-swara-set sibling groups using the pairwise cosine-similarity check already established for Phase 3 (Asavari/Jaunpuri, Marwa/Puriya, and the wider families they belong to — e.g. Asavari/Jaunpuri/Darbari Kanada/Adana share a thaat family, and Marwa/Puriya have a third sibling, Sohni, not yet in the database). This turns "which ragas actually need Phase 8 to disambiguate" into a known, documented list rather than something discovered ad hoc later, and keeps Phase 3's honest-tie reporting accurate as coverage grows — new ragas may introduce new 1.0-similarity collisions the tie-reporting logic needs to catch.

### Phase 8 — Sequential/melodic movement analysis: Aroha/Avroha, pakad (Arun's addition, 2026-07-08)
The current histogram approach is order-blind — it only measures how much time is spent on each note, not what sequence they occur in. This means ragas sharing the same note set but distinguished by melodic movement (aroha/avroha shape, vadi/samvadi emphasis, pakad) can be indistinguishable to a pure histogram classifier, and it's why "static" feedback in Phase 7's learning mode could only ever confirm note choice, never direction/order (e.g. it can't tell Sa-Re-Ga apart from Sa-Ga-Re — same histogram, wrong movement). This is not new scope — `config.py`'s schema already has unused `aroha`/`avroha`/`pakad`/`characteristic_phrases` fields, and it's what `instructions.md` originally called for.

Requires new building blocks beyond what exists today: turning the continuous pitch stream into discrete note events (onset/offset segmentation — the pipeline currently only produces frame-by-frame pitch, not "note X held from time A to B"), then matching that note sequence against each raga's aroha/avroha/pakad (start with something simple like n-gram or edit-distance matching before reaching for HMMs, which is what `instructions.md` originally suggested). Ornamentation (meend/gamak) will make segmentation noisy — expect this to need its own tuning pass. Depends on Phase 1 (schema) and should be validated against Phase 2's eval harness rather than by ear, same lesson as the rest of the classifier work. Feeds directly into Phase 7's learning-mode feedback once it exists.

**Output should be a caveated lean, not a hard flip (Arun's addition, 2026-07-08):** A pakad match will necessarily be approximate/fuzzy (real performances ornament around the textbook phrase), so it's weak evidence, not proof. When pakad/aroha matching is used to break a histogram tie (e.g. Asavari vs Jaunpuri, Marwa vs Puriya from Phase 3's honest-tie list), the output should stay hedged — "Probably Jaunpuri (Asavari also plausible)" — rather than switching to a confident single answer just because one approximate phrase match was found. This mirrors how even trained musicians stay tentative on these close pairs until much more evidence accumulates (see discussion on human raga recognition, session log below).

## 5. Working style going forward (to avoid repeating the stall)

- Treat this file and `CLAUDE.md` as the two things to read at the start of any session.
- Tackle one phase at a time; don't let a session wander across phases.
- After any change to classifier logic, run it against the eval harness (once Phase 2 exists) before deciding it "worked."
- Commit and push after each phase (or meaningful sub-step), with a commit message describing what changed and why — not just "fix."
- Update this file's status section (or add a dated log entry below) at the end of each session so the next session — or the next AI tool, if that ever happens again — picks up from an accurate description instead of stale assumptions.

## 6. Phase 3 findings (2026-07-08)

Ran an ablation study (classifier with each hack on vs. off, against the identical synthetic corpus `eval_harness.py` uses) rather than guessing:

- **`main_22.py`'s forbidden-note penalty** (flat 0.15 multiplier if a forbidden semitone exceeds 4% energy): **180/189 with, 180/189 without — zero difference.** Removed. Caveat worth remembering: the eval corpus only renders *correct* performances (no forbidden-note contamination), so this shows the penalty wasn't earning anything on clean input, not that it could never help on a messier real recording. If real audio (or noisier synthetic tests) later show specific raga pairs getting confused via forbidden-note bleed, this is the first thing worth reintroducing — with a number behind it this time.
- **`main.py`'s Yaman similarity bonus** (+0.15 if high-Ni energy present): **54/54 with, 54/54 without — zero difference.** Removed. Yaman already wins every trial on histogram similarity alone since Phase 2's weighted-histogram fix.
- **`main.py`'s `check_tie()` 90-second Bhupali lock:** not ablation-tested directly (it's stateful/time-based, not a fit for one-shot classify() calls), but the case for removal is strong by inference: the lock exists specifically to patch a classifier that gets "stuck" confusing Yaman/Bhupali, and the eval harness now shows **zero cross-confusion between Yaman and Bhupali** in any of 54 trials, at any tonic, under jitter, post Phase 2. The premise the lock was patched against no longer reproduces. Removed.

Net effect: `main.py` and `main_22.py` are both back down to plain cosine similarity against `config.py`'s weighted histograms, with no hand-tuned bonuses/penalties/locks. Re-ran the full eval harness after the changes: **still 180/189 (95.2%)**, confirming no regression from removing all three hacks in one pass.

**On the two other Phase-3-tagged notes above (two-stage elimination filter, honest-tie reporting):** re-measured cosine similarity between raga reference histograms after the weighted-histogram fix — Asavari/Jaunpuri dropped from the documented 1.0000 tie to **0.9365**, and Marwa/Puriya dropped out of near-tie range entirely (no pair besides Yaman/Bhupali at 0.8555 and Asavari/Jaunpuri even shows up above 0.85 now). The specific 1.0000 collisions those two notes were written against no longer exist. Decision: **defer both to Phase 8** (where sequence-matching is the real fix) rather than build interim scaffolding for a mostly-resolved problem — Asavari/Jaunpuri's residual 0.9365 similarity and the one known avroha-collision failure mode are the concrete thing Phase 8 should ultimately resolve.

## 7. Session log

- 2026-07-08 — Phase 5 done. Wired up `FeatureExtractor.auto_detect_tonic()` in `main_22.py`: the tonic prompt now accepts a blank answer to trigger auto-detection (median method) instead of requiring a typed Hz value. Added file-based input as an alternative to live mic (`run_file_mode()`, using `librosa.load()` — already a project dependency, no new one added) — you can now point the tool at an audio file instead of only listening live, closing the gap called out in `PROJECT_PLAN.md` section 1 ("or could take a dropped-in music file"). File mode processes the whole file once and prints a one-shot summary instead of the live 120s dashboard loop, since a file has a known finite length. Found and fixed a real bug during testing (not just a theoretical review): when auto-detecting tonic from a file, the opening calibration segment (e.g. a held Sa) was being folded into the *same* histogram used for classification, so it could dominate the result — verified with a synthetic test file (5s held Sa + Yaman aroha/avroha) where this caused the Sa segment to consume 89% of the histogram and misclassify as Puriya. Fixed by excluding the calibration window from the classification chunks when auto-detecting (mirrors how live mode already discarded its calibration chunks correctly). After the fix, tested against clean and short/silent synthetic files — no crashes, and manual-tonic mode with a clean performance file classifies correctly (100% Yaman). One honest residual: auto-detected tonic on a synthetic test landed ~30 cents off the true tonic (223.86 Hz vs. 220 Hz), which alone was enough to push a borderline case to a different raga (Puriya instead of Yaman) — this is a precision ceiling of the existing autocorrelation/median-based `auto_detect_tonic()` algorithm, not something this phase was scoped to fix (Phase 5 = wire it up, not redesign it); manually-entered tonic remains more reliable. Re-ran `eval_harness.py` after all changes: still 180/189 (95.2%), no regression (expected, since `RaagaClassifier.classify()` itself wasn't touched). Rewrote `CLAUDE.md`'s Running/Architecture sections to describe the new prompts and the auto-detect precision caveat.
- 2026-07-08 — Phase 4 done. Deleted `main.py` (its 2-raga registry was a strict subset of `main_22.py`'s, and its one unique feature, the tie-break lock, was already found to measure zero benefit and removed in Phase 3). `main_22.py` is now the sole entry point. Rewrote `CLAUDE.md` end to end — it was still describing the pre-Phase-1 three-database, two-entry-point state, which was actively misleading for any future session. Updated `PROJECT_PLAN.md` section 2 to strike through the now-resolved problems (drifted databases, ad hoc hacks, two entry points) rather than rewriting history. Re-ran `eval_harness.py` after the deletion: still 180/189 (95.2%), no regression. Also resolved a discrepancy the user had introduced directly into this plan file (two Phase-3-tagged design notes — a two-stage forbidden-note elimination filter, and honest-tie reporting for near-identical raga pairs — that I hadn't seen or built): re-measured cosine similarity between raga reference histograms and found the specific 1.0000 ties those notes were written against no longer exist post the Phase 2 weighted-histogram fix (Asavari/Jaunpuri now 0.9365, Marwa/Puriya no longer even near-tied). User decided to defer both ideas to Phase 8 rather than build interim scaffolding for a mostly-resolved problem.
- 2026-07-08 — Phase 3 done. Ablation-tested all three hardcoded hacks (`main.py`'s Yaman similarity bonus, its 90-second Bhupali lock, `main_22.py`'s forbidden-note penalty) against the eval harness — all three measured zero accuracy benefit once Phase 2's weighted histograms were in place, so all three were removed. Re-ran the eval harness after removing them: still 180/189 (95.2%), confirming no regression. Full ablation numbers are in section 6 above.
- 2026-07-08 — Migrated project from Windsurf to Claude Code. Fixed a stray git repo rooted at the home directory (was tracking unrelated personal files/SSH keys); re-initialized git scoped to the project folder, added `.gitignore`, set up SSH auth, pushed initial commit to `github.com/arunathavale/RagaDetector`. Reviewed the codebase and wrote this plan. No code changes yet — Phase 1 (database consolidation) is the next actual coding step.
- 2026-07-08 — Phase 2 started. (1) Built `eval_harness.py`: synthesizes sine-tone renditions of each raga's aroha/avroha/pakad from `config.RAAGA_DATABASE`, runs them through the real pitch-extraction pipeline (no mic needed), and scores `main_22.py`'s classifier. (2) Decided on a real-recording source: the Saraga Hindustani dataset (MTG, CC-BY-NC-SA 4.0) — confirmed by inspecting its GitHub file listing that it covers 6 of 7 target ragas (Yaman, Bhairav, Bhupali, Marwa, Puriya, Asavari as "Komal Rishav Aasavari" — variant name, needs verification) but has no Jaunpuri recordings. Considered `compmusic_raga` (30-raga Hindustani benchmark, CC-BY 4.0) as an alternative/supplement but its audio is gated behind a Zenodo access request. Downloading Saraga audio requires a free personal API token from dunya.compmusic.upf.edu (account signup only the user can do) plus post-download segmentation, since recordings are full multi-raga concerts, not pre-cut labeled clips. **User decided to hold off on real audio and keep iterating on the synthetic harness instead.**
- 2026-07-08 — Tried Option A (cheap fix) on the Jaunpuri/Puriya ceiling: replaced `config.py`'s flat `create_swara_histogram` (which just marked "present/absent") with `compute_swara_weights` + `create_weighted_swara_histogram`, weighting each swara by how often it occurs across a raga's own aroha+avroha+pakad, with a vadi/samvadi emphasis boost. Histograms are now built in a post-construction pass over `RAAGA_DATABASE` instead of inline per-entry, so weights derive from each raga's own data instead of being hand-typed. Result: **eval_harness.py jumped from 71.4% to 95.2% (180/189)** — Jaunpuri and Puriya both went from 0% to 100%. One residual gap: Asavari's avroha-only rendition still misclassifies as Jaunpuri 9/9 times, because Asavari's and Jaunpuri's `avroha` lists in `config.py` were entered as literally identical swara sequences — a data-authoring artifact (these two ragas are genuinely close relatives, same thaat), not a new classifier bug. Worth revisiting if/when the aroha/avroha data gets a more careful musicological pass. Phase 8 (sequence-order matching) is no longer an urgent blocker for these two ragas specifically, though it remains the more correct long-term fix per the original plan.
- 2026-07-08 — Strengthened `eval_harness.py`: instead of one deterministic pure-tone pass, each (raga, sequence type) is now rendered across 3 tonics × 3 trials with random per-note pitch jitter (±15 cents, simulating vocal imprecision) — 189 total cases — plus a confusion matrix. Result: 135/189 (71.4%), and the failure mode is now proven structural rather than a fluke: Jaunpuri → misclassified as Asavari 27/27 times, Puriya → misclassified as Marwa 27/27 times, with zero exceptions across every tonic/jitter combination. These pairs share literally the same `required_notes` set in `config.RAAGA_DATABASE`, so the order-blind flat histogram cannot distinguish them at all — this is a ceiling on accuracy, not classifier mistuning, until Phase 8 (sequence/aroha-avroha-aware matching) or Phase 3 (richer per-raga histogram weighting) is tackled. Everything else (Yaman, Bhupali, Bhairav, Asavari, Marwa) is 100% robust to jitter and tonic shift. **Side note:** an earlier `pip install mirdata` attempt (while researching real-recording sources) partially upgraded numpy/scipy/scikit-learn/librosa in the base conda environment before failing partway through, which silently broke this project's pinned dependencies. Caught it when `eval_harness.py` started throwing `numpy has no attribute 'long'` from librosa/numba; reinstalled the exact versions pinned in `requirements.txt` to fix it. Worth remembering if anything in this environment seems to unexpectedly break: check `pip show` against `requirements.txt` first.
- 2026-07-08 — Phase 1 done. Expanded `config.py::RAAGA_DATABASE` from 2 to all 7 ragas (`Bhairav`, `Asavari`, `Jaunpuri`, `Marwa`, `Puriya` added, each with aroha/avroha/vadi/samvadi/pakad/prahar/forbidden_notes/required_notes/characteristic_phrases). Deleted the inline `RAGA_REGISTRY` + `generate_idealized_histogram()` from both `main.py` and `main_22.py`; both now import `RAAGA_DATABASE` from `config.py` (`main.py` narrows it to `{Yaman, Bhupali}`, matching its original 2-raga scope). `main_22.py`'s forbidden-note penalty check now derives semitone indices from `config.SWARA_MAPPING` instead of hardcoded index lists. Smoke-tested both classifiers against the shared database without audio hardware — imports clean, `config.py`'s own `validate_config()` passes, `classify()`/`check_tie()` run without error on synthetic histograms. Not yet run against live mic input. Next: Phase 2 (eval harness) before touching any classifier logic again.
