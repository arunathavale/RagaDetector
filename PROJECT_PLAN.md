# RagaDetector — Project Plan

Last updated: 2026-07-08

This file is meant to be the anchor document for this project — read it at the start of any Claude Code session (alongside `CLAUDE.md`) before making changes, and update it as milestones are hit or the plan changes.

## 1. What this project is

A CPU-only Hindustani classical Raaga identifier. It listens to live microphone audio or a dropped-in music file (Phase 5, 2026-07-08), extracts pitch, builds a tonic-relative 120-bin pitch-class histogram, and compares it against reference Raaga templates via cosine similarity to guess which Raaga is being performed.

## 2. Where things actually stand today

~~Two working prototype entry points exist~~ **As of Phase 4 (2026-07-08), one entry point:**

- `main_22.py` — a 7-raga "studio" mode (Yaman, Bhupali, Bhairav, Asavari, Jaunpuri, Marwa, Puriya) where you tell it which raga you intend to sing, and it shows a live PASS/FAIL comparison against that target.
- `main.py` (the old 2-raga Yaman/Bhupali mode with tie-breaking/lock logic) was retired in Phase 4 — its registry was a strict subset of `main_22.py`'s, and its one distinguishing feature (the lock hack) was ablation-tested and removed in Phase 3 for measuring zero benefit.

It runs for a fixed 120-second window and prints a JSON summary at the end. This is useful for testing, but it's a test-harness shape, not yet a "just detect whatever raga I'm singing" tool.

The honest problems, found while reviewing the code just now:

- ~~**Three separate, drifted Raaga databases.**~~ **Resolved in Phase 1 (2026-07-08).** `config.py::RAAGA_DATABASE` (the richest schema — aroha, avroha, pakad, vadi, samvadi, prahar) now covers all 7 ragas and is the single source both `main.py` and `main_22.py` import from; the inline registries and idealized-histogram generators were deleted from both.
- ~~**Hand-tuned, per-raga hacks instead of a general classifier.**~~ **Resolved in Phase 3 (2026-07-08).** `main.py`'s Yaman similarity bonus, its 90-second Bhupali lock, and `main_22.py`'s flat forbidden-note penalty were all ablation-tested against the eval harness and removed (each measured zero accuracy benefit once Phase 2's weighted histograms were in place). Both entry points now run plain cosine similarity with no hand-tuned bonuses/penalties/locks.
- ~~**Tonic (Sa) is entered by hand every run.**~~ **Resolved in Phase 5 (2026-07-08).** `FeatureExtractor.auto_detect_tonic()` is now wired up — leaving the tonic prompt blank triggers auto-detection instead of requiring a typed Hz value. Caveat: its precision is limited (~30 cents off on a clean synthetic test), which can occasionally push a borderline case to a different raga; a manually-entered tonic is still more reliable when accuracy matters.
- **No objective evaluation.** There's no labeled dataset or test set to score classifier accuracy against. Progress so far has likely been judged by singing live and eyeballing the dashboard, which makes it very hard to tell whether a change actually helped or just fixed the one case you were staring at (this is almost certainly why work with Devin/Gemini started "spinning wheels" — every fix was a one-off reaction, with nothing to check it against, so old cases could quietly break while a new one got patched).
- **`instructions.md` is aspirational, not a status report.** It describes HMMs, SVMs, ensemble classifiers, tala detection, TensorFlow Lite, a plugin architecture — none of which exist in the code. It's useful as a reference for future direction, but it doesn't reflect what's built.
- **Housekeeping debt.** `raga_detector.log` had grown to 265KB and was being tracked by git before we added `.gitignore`; `__pycache__` likewise. ~~Two entry points with overlapping but diverging logic is itself a maintenance cost.~~ Resolved in Phase 4 — down to one entry point.

None of this means the project is in bad shape — the core pipeline (pitch detection → cents conversion → histogram → cosine similarity) is a legitimate, well-understood approach for this problem, and it's already running end-to-end on live audio. The issue is architectural drift and a lack of a feedback loop to measure progress, not a fundamentally broken idea.

## 3. Why progress stalled

Most likely pattern: each AI coding session (Devin, Gemini) was handed a live symptom ("it keeps guessing Bhupali," "Yaman never gets picked") and asked to fix it, with no persistent ground-truth test set and no single source of truth for the raga definitions. Each tool fixed the symptom in front of it by adding a special case to whichever file it happened to be looking at, which is how you end up with three databases and several hardcoded bonuses/penalties. Without an eval set, there was no way to tell if a "fix" was a real improvement or an overfit to one test.

## 4. Recommended approach: fix the process, then the code

The methodical fix is to stop patching symptoms and put a few structural things in place first, in this order. Each phase is small enough to be one or two focused Claude Code sessions.

### Phase 1 — Consolidate to one source of truth
Pick a single Raaga database schema (recommend `config.py::RAAGA_DATABASE`'s richer schema — aroha, avroha, pakad, vadi, samvadi, forbidden/required notes — since it's the most complete) and make both entry points import from it. Delete the inline registries in `main.py` and `main_22.py`. Expand it to cover the 7 ragas `main_22.py` currently has inline, at minimum.

### Phase 2 — Build a real evaluation harness
Before touching classifier logic again, create a small labeled test set — even a handful of short recordings (or synthetic tone sequences generated from each raga's aroha/avroha) with known correct answers — and a script that runs the classifier against all of them and reports accuracy. This turns "does this help?" from a guess into a number you can compare run to run. This is the single highest-leverage step to stop the spinning-wheels pattern.

### Phase 3 — Replace ad hoc hacks with justified logic
With the eval harness in place, revisit the hardcoded bonuses/penalties/lock heuristics. Either remove them and see how much accuracy the plain cosine-similarity approach gets on its own, or keep only the ones that measurably improve the eval score — and document why each one exists.

**Two-stage classification (Arun's addition, 2026-07-08):** Turn "forbidden notes" from a soft similarity penalty into a first-pass elimination filter — if a raga's forbidden swara shows sustained energy in the live histogram above a threshold, drop that raga from the candidate set entirely before running cosine similarity on whatever remains. This is musicologically sound (several ragas are defined partly by which notes they exclude, e.g. Bhupali has no Ma/Ni) and should narrow the field faster than a penalty multiplier does. Caveat to test against the eval harness: the elimination threshold needs to tolerate pitch-detection noise (a single stray frame misreading a forbidden note shouldn't disqualify the correct raga) — likely wants the same kind of sustained/energy-over-time threshold the current 4% penalty check uses, not a one-frame trigger.

**Known histogram-identical pairs — honest-tie reporting (Arun's addition, 2026-07-08):** Measured directly against the Phase-1-consolidated `config.py` database: Asavari vs Jaunpuri and Marwa vs Puriya both score cosine similarity 1.0000 against each other — completely indistinguishable by the current flat-histogram method, because they share the same note set and differ only in aroha shape (Asavari's aroha skips Ga_komal on the way up; Jaunpuri's doesn't), vadi/samvadi emphasis (Marwa: Re_komal/Dha_shuddha vs Puriya: Ni_shuddha/Ga_shuddha, with Puriya characteristically starting from the lower Ni), and pakad — none of which the classifier currently looks at. Until Phase 8's sequence/pakad matching exists to actually resolve these, the classifier should detect when top candidates' reference histograms are near-identical (cosine similarity above ~0.98) and report them jointly, e.g. "Asavari or Jaunpuri — indistinguishable by current method," rather than silently guessing one and being wrong roughly half the time. Phase 8 is the real fix; this is the honest interim behavior until then.

### Phase 4 — Retire `main.py`, keep one entry point
Arun's read (and it holds up): `main.py`'s 2-raga registry (Yaman, Bhupali) is a strict subset of `main_22.py`'s 7-raga registry, and `main_22.py` is the more recently edited file. `main.py`'s only unique content is its tie-break/lock hack, which Phase 3 should be evaluating (and likely discarding or replacing) anyway. Plan: once Phase 1–3 are done and `main_22.py` is confirmed stable, delete `main.py` rather than merging it — there's nothing in it worth preserving beyond what the eval harness already tells you about the lock heuristic.

### Phase 5 — Quality-of-life
Wire up `auto_detect_tonic()` so tonic doesn't need to be typed in by hand each run. Add support for dropping in a music file (not just live mic) as an input source, since that's part of the original goal.

### Phase 6 — Only now, consider the aspirational features
Things like pakad (phrase) matching, HMMs, ensemble classifiers, tala detection from `instructions.md` are legitimate v2 ideas, but only worth adding once there's a reliable way (the eval harness) to tell whether they actually improve results.

### Phase 7 — Practice/learning mode (Arun's addition, 2026-07-08)
Extend the existing "studio" comparison (already in `main_22.py`: pick a raga, sing, see live vs. target) into an actual learning tool that points out specific variations rather than just a coarse bar chart and PASS/FAIL. Depends on Phase 1 (needs the richer `config.py` schema — aroha/avroha/pakad, not just a flat histogram) and ideally Phase 2's eval harness for validating that the feedback given is musically correct. Not a separate project — it reuses the entire audio/pitch/histogram/database pipeline; only the "what do we do with the comparison" step differs from guess-the-raga mode. Candidate feedback types: per-swara sharp/flat deviation in cents (needs finer resolution than the current 12-bucket semitone sum), accidental forbidden-note hits, aroha/avroha sequence adherence.

### Sequencing note: expand raga coverage before Phase 8 (Arun's addition, 2026-07-08)
Decision: grow the raga database well beyond the current 7 before starting Phase 8's sequence/pakad work, rather than building sequence-matching machinery around a small set and re-doing it later. While expanding, explicitly catalog same-swara-set sibling groups using the pairwise cosine-similarity check already established for Phase 3 (Asavari/Jaunpuri, Marwa/Puriya, and the wider families they belong to — e.g. Asavari/Jaunpuri/Darbari Kanada/Adana share a thaat family, and Marwa/Puriya have a third sibling, Sohni, not yet in the database). This turns "which ragas actually need Phase 8 to disambiguate" into a known, documented list rather than something discovered ad hoc later, and keeps Phase 3's honest-tie reporting accurate as coverage grows — new ragas may introduce new 1.0-similarity collisions the tie-reporting logic needs to catch.

### Phase 8 — Sequential/melodic movement analysis: Aroha/Avroha, pakad (Arun's addition, 2026-07-08)
The current histogram approach is order-blind — it only measures how much time is spent on each note, not what sequence they occur in. This means ragas sharing the same note set but distinguished by melodic movement (aroha/avroha shape, vadi/samvadi emphasis, pakad) can be indistinguishable to a pure histogram classifier, and it's why "static" feedback in Phase 7's learning mode could only ever confirm note choice, never direction/order (e.g. it can't tell Sa-Re-Ga apart from Sa-Ga-Re — same histogram, wrong movement). This is not new scope — `config.py`'s schema already has unused `aroha`/`avroha`/`pakad`/`characteristic_phrases` fields, and it's what `instructions.md` originally called for.

Requires new building blocks beyond what exists today: turning the continuous pitch stream into discrete note events (onset/offset segmentation — the pipeline currently only produces frame-by-frame pitch, not "note X held from time A to B"), then matching that note sequence against each raga's aroha/avroha/pakad (start with something simple like n-gram or edit-distance matching before reaching for HMMs, which is what `instructions.md` originally suggested). Ornamentation (meend/gamak) will make segmentation noisy — expect this to need its own tuning pass. Depends on Phase 1 (schema) and should be validated against Phase 2's eval harness rather than by ear, same lesson as the rest of the classifier work. Feeds directly into Phase 7's learning-mode feedback once it exists.

**Output should be a caveated lean, not a hard flip (Arun's addition, 2026-07-08):** A pakad match will necessarily be approximate/fuzzy (real performances ornament around the textbook phrase), so it's weak evidence, not proof. When pakad/aroha matching is used to break a histogram tie (e.g. Asavari vs Jaunpuri, Marwa vs Puriya from Phase 3's honest-tie list), the output should stay hedged — "Probably Jaunpuri (Asavari also plausible)" — rather than switching to a confident single answer just because one approximate phrase match was found. This mirrors how even trained musicians stay tentative on these close pairs until much more evidence accumulates (see discussion on human raga recognition, session log below).

## 5. Working style going forward (to avoid repeating the stall)

- Treat this file and `CLAUDE.md` as the two things to read at the start of any session.
- Tackle one phase at a time; don't let a session wander across phases.
- After any change to classifier logic, run it against the eval harness (once Phase 2 exists) before deciding it "worked."
- Commit and push after each phase (or meaningful sub-step), with a commit message describing what changed and why — not just "fix."
- Update this file's status section (or add a dated log entry below) at the end of each session so the next session — or the next AI tool, if that ever happens again — picks up from an accurate description instead of stale assumptions.

## 6. Phase 3 findings (2026-07-08)

Ran an ablation study (classifier with each hack on vs. off, against the identical synthetic corpus `eval_harness.py` uses) rather than guessing:

- **`main_22.py`'s forbidden-note penalty** (flat 0.15 multiplier if a forbidden semitone exceeds 4% energy): **180/189 with, 180/189 without — zero difference.** Removed. Caveat worth remembering: the eval corpus only renders *correct* performances (no forbidden-note contamination), so this shows the penalty wasn't earning anything on clean input, not that it could never help on a messier real recording. If real audio (or noisier synthetic tests) later show specific raga pairs getting confused via forbidden-note bleed, this is the first thing worth reintroducing — with a number behind it this time.
- **`main.py`'s Yaman similarity bonus** (+0.15 if high-Ni energy present): **54/54 with, 54/54 without — zero difference.** Removed. Yaman already wins every trial on histogram similarity alone since Phase 2's weighted-histogram fix.
- **`main.py`'s `check_tie()` 90-second Bhupali lock:** not ablation-tested directly (it's stateful/time-based, not a fit for one-shot classify() calls), but the case for removal is strong by inference: the lock exists specifically to patch a classifier that gets "stuck" confusing Yaman/Bhupali, and the eval harness now shows **zero cross-confusion between Yaman and Bhupali** in any of 54 trials, at any tonic, under jitter, post Phase 2. The premise the lock was patched against no longer reproduces. Removed.

Net effect: `main.py` and `main_22.py` are both back down to plain cosine similarity against `config.py`'s weighted histograms, with no hand-tuned bonuses/penalties/locks. Re-ran the full eval harness after the changes: **still 180/189 (95.2%)**, confirming no regression from removing all three hacks in one pass.

**On the two other Phase-3-tagged notes above (two-stage elimination filter, honest-tie reporting):** re-measured cosine similarity between raga reference histograms after the weighted-histogram fix — Asavari/Jaunpuri dropped from the documented 1.0000 tie to **0.9365**, and Marwa/Puriya dropped out of near-tie range entirely (no pair besides Yaman/Bhupali at 0.8555 and Asavari/Jaunpuri even shows up above 0.85 now). The specific 1.0000 collisions those two notes were written against no longer exist. Decision: **defer both to Phase 8** (where sequence-matching is the real fix) rather than build interim scaffolding for a mostly-resolved problem — Asavari/Jaunpuri's residual 0.9365 similarity and the one known avroha-collision failure mode are the concrete thing Phase 8 should ultimately resolve.

- 2026-07-10 — Redid the pyin test fairly, and it **corrects** rather than confirms the "unfair test" theory from the previous entry. Added `test_continuous_pyin.py`: runs `librosa.pyin()` ONCE, continuously, across the whole file with a proper ~10ms hop (matching `SWARA_EXTRACTION_SPEC.md`'s recommendation) instead of on isolated ~23ms chunks with zero continuity between them. Result on the same ground-truth file (Bhimsen Joshi, tonic 110.79 Hz, should be Yaman): detection rate improved modestly (10.0% vs. the earlier chunked test's 8.1%), but classification is still wrong, and arguably *further* from truth than before - it now picks Bhupali (the earlier flawed test at least landed on Jaunpuri), with `Ma_shuddha` wildly over-represented (+27.5%). Conclusion: continuity helps a little but isn't the real fix - pYIN's confidence scoring is fundamentally source-agnostic (rewards *any* stable, confidently-pitched content, with no notion of "prefer the foreground melody"), and more temporal context doesn't change what it's confident *about*. The original diagnosis from the first pyin test (drone-lock, not a methodology artifact) holds up. Kept `test_continuous_pyin.py` as reusable tooling in case hop-length/threshold tuning is worth revisiting later, but not pursuing pyin further as the fix for the accompaniment problem - CREPE or genuine source separation remain the real candidates if this gets picked up again, not a pyin methodology fix.
- 2026-07-10 — Arun brought an external architecture review (`SWARA_EXTRACTION_SPEC.md`, a 9-stage pipeline design citing the same CompMusic/MTG research line behind Essentia's `TonicIndianArtMusic`) for a second opinion on the project's approach. Assessment: largely validates decisions already made independently this session (whole-recording pitch histogram folded to one octave = `find_optimal_tonic.py`; human-correctable rather than 100%-automatic = `confirm_or_override_tonic()`; don't hard-classify every frame = the exact mechanism behind the forbidden-note question a few messages earlier) rather than exposing a wrong architecture. Two genuinely new, actionable ideas taken from it and implemented in `find_optimal_tonic.py`: (1) **drone-interval tonic scoring** (`score_drone_priors()` / `find_best_tonic_by_drone_priors()`, `--drone` mode) - scores candidate tonics by whether a Sa-Pa (fifth) or Sa-Ma (fourth) drone dyad is present, a raga-agnostic structural signal from how a tanpura is actually tuned, independent of (and complementary to) the existing raga-matching searches; (2) **within-recording stability check** (`check_tonic_stability()`, now printed after every run) - splits the file into 4 segments and independently finds each one's best drone-prior tonic, since tonic essentially never changes mid-performance, so disagreement across segments flags a pitch-tracking problem in that portion rather than a real tonic shift.

  Validated against known answers: on the highest-confidence file (Bhimsen Joshi, 8438 detections, classifier-validated 110.79 Hz), the drone-prior whole-file estimate landed at 102.18 Hz - same rough neighborhood, not exact (~140 cents off), with 2 of 4 segments independently agreeing on that value. **But the stability check flagged real disagreement (470 cent spread) even on this best-available data** - segments 1 and 2 landed at 126.53 Hz and 192.89 Hz. Same mixed picture on a live-mic Bhairav recording (Sa-Pa priors pointed to 198.55 Hz, some segments over 500 cents apart). Honest conclusion: this is a real, working, complementary diagnostic - not a solved problem. The stability check is doing exactly what it's supposed to (surfacing genuine uncertainty instead of reporting false confidence), which is itself the valuable part, more than any single number it produces. Also flagged as likely wrong from earlier tonight: the `pyin` test that showed 8.1% detection and drone-lock-on was run on isolated ~23ms chunks with zero temporal continuity, which defeats the Viterbi-decoding mechanism pYIN actually relies on to disambiguate a moving melody from a stable drone - that test should be redone in continuous whole-file mode before concluding pYIN doesn't work here, rather than reaching for CREPE (heavier, deep-learning) on the strength of an unfair test. Not done yet - next candidate for this same thread. Nyas/stable-region segmentation (the spec's stages 6-8, matching this project's own Phase 8) remains the bigger, not-yet-started piece.
- 2026-07-09 — Fixed a real inconsistency in `run_file_mode()`: when tonic was manually entered, `performance_chunks = chunks` used the *entire* file including the first `TONIC_DETECTION_SECONDS`; the auto-detect path correctly excluded that same window (it's a calibration reference from `save_recording()`'s capture flow, not performance content). Since re-analyzing a saved recording with a manually-specified tonic is now a normal, recommended workflow, this meant `run_file_mode()` and `find_optimal_tonic.py` could give *different, contradictory* answers for the identical file and tonic - caught exactly this way: `find_optimal_tonic.py` found Bhairav winning decisively at 140.39 Hz for `Bhairav_20260709_201913.wav`, but `run_file_mode()` at the same tonic returned Yaman. Root cause confirmed via `main_22.py:426-429`. Fix: the calibration-window trim is now applied unconditionally, before the manual/auto-detect branch, so chunk selection can no longer depend on how the tonic was obtained. Re-tested: `run_file_mode()` now agrees with `find_optimal_tonic.py` (Bhairav 100%, Marwa 62.6%). Re-ran `eval_harness.py`: unaffected (180/189, file mode isn't exercised by the synthetic suite). Separately: initially misdiagnosed this session's low PROJECT_PLAN.md line count (119) as a sign of lost content from an external edit/revert - false alarm, caused by not reading the full file before concluding from `wc -l`/`tail` alone. Noted here as a lesson, not a real incident.
- 2026-07-09 — Added `--blind` mode to `find_optimal_tonic.py` (`find_best_tonic_and_raga()`): searches every (tonic, raga) combination simultaneously instead of requiring the raga be specified up front. Validated against the clean Bhimsen Joshi file: correctly and independently rediscovers Yaman at ~110.79 Hz as the top candidate with no raga hint given, exactly matching the targeted-search result. **But it failed the same test on a live-mic recording**: its #1 pick for `Yaman_20260709_135012.wav` was Asavari at 151.34 Hz (+56.4% gap) - a *stronger*, more confident match than Yaman gets at its own best tonic (185.25 Hz, +37.5%), even though the recording is actually Yaman. Conclusion: blind search is not reliable as a primary method - a coincidentally strong wrong (tonic, raga) interpretation can outrank the true one on noisier real data. **The reliable method remains the targeted search** (specify the intended raga, which is normally known since the user chooses what to sing) - that one has a clean 3/3 track record across every file tested this session. Blind search is useful as a secondary sanity check (does the intended raga even appear in the top 10 candidates) but its top pick should not be trusted on its own. Investigated Essentia's `TonicIndianArtMusic` (purpose-built multipitch tonic ID from the same MTG/CompMusic lab behind Saraga) as a potentially more principled long-term approach - installed cleanly into an isolated venv, but every attempt hung indefinitely (zero CPU progress, not just slow) at the exact same point: the base `import essentia` statement, before ever reaching `essentia.standard` or `TonicIndianArtMusic` specifically. Diagnosed with `lsof` on the stuck process: it was loading a bizarre dependency chain for an audio library - `AppKit.framework` (macOS's GUI framework), `SystemAppearance.bundle` (dark mode/vibrancy rendering), `SFSymbols.framework`, system fonts, keyboard layout bundles, icon services. This build of essentia bundles FFmpeg-style video codec libs (`libavcodec`, `libx265`, `libvpx`) and font-rendering libs (`libfreetype`, `libharfbuzz`) that appear to pull in macOS GUI frameworks during import - a known category of macOS packaging issue (libraries expecting an app/GUI context hanging when loaded from a plain headless terminal), not something specific to this project's code. Not worth debugging further right now given a reliable working method already exists (the targeted tonic search above) - parked. If revisited later, worth trying: Homebrew install instead of the PyPI wheel, an older essentia version, or checking the MTG essentia GitHub issues for this specific hang signature.
- 2026-07-09 — **Correction to the working theory**: earlier session-log entries leaned toward "recording setup (phone speaker + room + non-directional mic) is a real, independent contributing factor beyond tonic," based on a tonic sweep of only *one* of the two saved live-mic recordings (`Yaman_20260709_152450.wav`, best gap only +17.2%, notably weaker than the clean Bhimsen Joshi file's +56.3%). Arun asked directly whether "all recordings produce a correct Yaman result at the correct tonic," which prompted running `find_optimal_tonic.py` against the *other* live-mic recording too (`Yaman_20260709_135012.wav`, previously only tested at its original, likely-wrong 341.86 Hz auto-detected value). Result: at its empirically-best tonic (~185.25 Hz), it also correctly picks Yaman, with a +37.5% gap - much closer to the clean file's confidence than the other live session's +17.2%. **Corrected conclusion: tonic is the dominant factor, not just one contributing factor among several roughly-equal ones** - in all three files tested so far (2 live-mic, 1 clean reference), Yaman wins once the tonic is right. The margin does still vary (17.2% to 56.3%), so recording quality likely adds some noise on the margins, but it does not appear to be flipping results the way earlier entries implied. Lesson for future diagnostic work in this file: don't generalize a conclusion from a single test case when a second, comparable case is one command away - this was an avoidable overclaim caught by a direct question rather than by checking completeness first.
- 2026-07-09 — Added `find_optimal_tonic.py`: an exact, efficient way to find the tonic that minimizes deviation from a target raga, replacing the earlier ad hoc brute-force sweep (which re-ran full pitch detection for every candidate tonic - wasteful, since pitch detection doesn't depend on tonic at all). Key insight: `cents = 1200*log2(pitch/tonic) mod 1200` means changing tonic is mathematically a **circular shift** of the pitch-class histogram - a classic circular cross-correlation / rotational-alignment problem, not something that needs re-detection per candidate. Detects pitches once, builds one "anchor" histogram (arbitrary fixed reference tonic), then tests all 120 possible 10-cent rotations per octave via cheap `np.roll()` calls - exact over the full bin resolution, not a coarse linear Hz sweep like before. Scores each candidate through the real `RaagaClassifier.classify()` (including its per-swara threshold - raw cosine similarity without that threshold gave a measurably different, wrong-ish answer when first tried) and ranks by **gap over the 2nd-best raga**, not raw score - `classify()`'s min-max normalization means the winning raga always shows exactly 100% regardless of confidence, so raw score can't distinguish a landslide from a coin-flip. Validated against the earlier manual sweep's known answer for the Bhimsen Joshi recording (170 Hz): the new tool corroborates that family (169.88 Hz, +55.5% gap) and finds an even stronger candidate at 110.79 Hz (+56.3% gap) using the full file rather than a middle-third segment. Confirms octave-invariance works correctly (110.79/221.58/443.15 Hz give bit-identical results).
- 2026-07-09 — Fixed a real ambiguity that had come up twice now: `input_tonic_sa_hz: 145.0` appeared in two separate sessions and it was impossible to tell from the output whether that meant "genuinely auto-detected as 145.0 Hz" or "detection failed entirely and silently fell back to `FALLBACK_TONIC_HZ` (also 145.0)" - Arun confirmed the second run was auto-detected, not typed, which made the ambiguity concrete rather than theoretical. Root cause: `FALLBACK_TONIC_HZ` (145.0) sits inside `TYPICAL_TONIC_RANGE_HZ` (100-500), so a failed detection looks exactly as "plausible" as a real one to the existing soft warning - there was no signal anywhere distinguishing the two cases. Fixed by threading a `was_detected` boolean through `detect_tonic()` (now returns `(tonic_hz, was_detected)`) into `confirm_or_override_tonic()` (now returns `(tonic_hz, source)` with `source` one of `'auto_detected'`, `'fallback_accepted'`, `'manual_override'`, or `'manual'` for the top-level typed case) - a real failed detection now prints an unmissable 🚫 warning distinct from the softer ⚠️ "detected but implausible" case, regardless of whether the fallback number happens to look reasonable. `tonic_source` is now recorded in the final JSON summary's `session_metadata`, so this ambiguity cannot recur even silently - every future session's output will say explicitly how its tonic was obtained. Tested all branches (genuine detection, fallback accepted, fallback overridden, detected-but-implausible, fallback-then-retry-succeeds) with simulated input - all correct. Re-ran `eval_harness.py`: unaffected (180/189).
- 2026-07-09 — Extended live mic mode's session window from 120 seconds to 5 minutes (`SESSION_DURATION_SECONDS` in `main_22.py`, replacing three separate hardcoded `120`/`120.0` occurrences - loop exit condition, dashboard "Time Remaining" display, final summary duration). Motivation: a 2-minute capture likely lands mostly in the alap (the slow, exploratory opening of a Hindustani vocal performance) before it settles into the fuller range of characteristic phrases that actually distinguish a raga - "wrong slice" risk, not a classifier problem. `ROLLING_WINDOW_SECONDS` (45s, controls the live dashboard's moment-to-moment responsiveness) is unchanged - it's a separate concern from total session length. Re-ran `eval_harness.py`: unaffected, as expected (it doesn't use this constant).
- 2026-07-09 — **Major bug found and fixed via testing against real audio, not synthetic data: `RaagaClassifier.classify()`'s noise threshold operated per-bin instead of per-swara, silently zeroing out entire histograms of real singing.** Context: after exhausting three pitch-detection methods (autocorrelation, yin, pyin) and a noise-floor-subtraction idea against Arun's own live-mic recording without success, downloaded a real reference recording (Raga Yaman sung by Bhimsen Joshi, from an Internet Archive compilation - see below for sourcing/rights discussion) to get a controlled comparison against a professionally-recorded, non-copyrighted-context file. That file's classification came back as a meaningless 3-way tie (Yaman/Bhupali/Bhairav all at exactly 14.3% = 1/7, the classifier's degenerate fallback). Root cause, found by inspecting the actual histogram: `classify()` did `cl[cl < 0.05] = 0.0` on the raw 120-bin histogram - a **per-bin** threshold. Real singing naturally spreads pitch across several bins within a swara's 10-bin range (vibrato, natural pitch wobble) rather than concentrating in one bin the way `eval_harness.py`'s synthetic sine tones do (even with jitter) - so a swara carrying a genuine 14% of the total (in this case `Ga_shuddha`, Yaman's own vadi) got divided across ~6-8 bins, none individually clearing 5%, and the *entire* histogram was zeroed to nothing. This bug was invisible to `eval_harness.py`'s whole test suite because synthetic tones never have that multi-bin spread. Fixed via `_apply_swara_threshold()`: zero a swara's whole 10-bin block only if its *aggregate* sum is below the threshold, not each bin independently. Re-ran `eval_harness.py` after the fix: still 180/189, no regression. Re-tested the Bhimsen Joshi recording at an empirically-swept correct tonic (~170 Hz, found via a tonic sweep that showed a sharp, isolated confidence peak): now correctly and confidently identifies as Yaman (100%, next-best Bhupali only 39.9%) across the full 272-second performance, real tabla/tanpura/harmonium accompaniment and all.

  However, re-testing Arun's *original* live-mic recording with the same fix changed its result (Asavari → Bhairav) but did not fix it to Yaman - the underlying per-swara numbers are identical to before the fix (the fix only changes classification-time thresholding, not histogram construction), and they show real elevated `Ma_shuddha`/`Pa`/`Ma_tivra` content, not a thresholding artifact. This is actually a clean, controlled result: the same classifier, same fix, correctly handles a properly-sourced file but not the live-mic capture - real evidence that the recording setup (phone speaker + room acoustics + non-directional laptop mic, discussed in adjacent session-log entries) is a genuine, independent contributing factor, on top of the threshold bug, not a substitute explanation for it.

  On sourcing the reference file: the Internet Archive item (`archive.org/details/31-ranjish-hi-sahi-mehdi-hasan`, a 24-track "Raga Yaman" compilation) has no visible rights/license statement despite the identifier's "non-copyrighted" framing Arun had in mind - it's a personal upload of commercially-released recordings by well-known artists (Mehdi Hasan, Lata Mangeshkar, Bhimsen Joshi, others), credited only to an uploader/collector, not cleared by rightsholders. Flagged this plainly; Arun confirmed proceeding anyway is fine for strictly private, non-commercial use. Downloaded the Bhimsen Joshi track specifically (`reference_audio/`, gitignored) since he's a genuinely serious classical vocalist, unlike the more ghazal/film-adjacent tracks by other artists in the same compilation (same "loosely based on the raga" caveat as the earlier Bollywood-songs discussion applies to those).

  Also worth noting: auto-detected tonic failed *again* on this completely clean file (648.53 Hz, correctly flagged by the existing plausibility warning) - confirming tonic auto-detection's unreliability was never primarily about room/mic noise; the "whatever's in the first 5 seconds" assumption fails regardless of source quality. The empirical tonic-sweep technique used here (scan a range of plausible tonics, look for a sharp confidence-gap peak) is a legitimate, repeatable diagnostic worth keeping in mind for future recordings with unknown tonic.

- 2026-07-09 — Added session recording (`save_recording()`): live mode's raw audio was previously processed and discarded in real time with no record kept anywhere - discovered this the hard way when trying to compare pitch-detection methods (`autocorrelation` vs. the already-present-but-unused `yin`) against "the last failed session" and realizing there was no file to replay. Every chunk for the whole live session (calibration + performance) is now accumulated separately from the analysis pipeline and written to `recordings/{intended_raga}_{timestamp}.wav` via `scipy.io.wavfile` (already a pinned dependency, no new install) when the session ends, regardless of pass/fail. Verified the file round-trips correctly through the exact same `load_file_chunks()` that `run_file_mode()` already uses, so any recorded session can be immediately re-analyzed in file mode - with a different pitch-detection method, for building empirical reference histograms (the real-recordings idea from earlier), or just to listen back. Added `recordings/` to `.gitignore` (personal audio, shouldn't be committed). Also worth noting as context for *why* this came up: Arun's music guru pushed back on the idea that Tabla/Tanpura/Harmonium accompaniment should interfere with swara identification at all - correctly, since trained musicians never have trouble with this. That reframed the fix from "remove the accompaniment" (source separation) to "use a more discerning pitch-tracking algorithm" (the voice/melody has no trouble being heard by a human ear even with accompaniment present) - `feature_extraction.py` already has a `yin`-method alternative to the currently-used `autocorrelation` default that's never actually been exercised; comparing the two head-to-head on a real recording is the natural next step once a recording exists to compare against.
- 2026-07-08 — Phase 7 started (partial): after walking through why the live "Top 3" fluctuated and a wrong tonic could scramble a whole session, Arun asked for a full-120-second actual-vs-theoretical comparison rather than just a single PASS/FAIL - which is literally two of Phase 7's three documented candidate feedback types ("per-swara deviation," "accidental forbidden-note hits"). Built `compute_swara_deviation_report()`: captures every pitch detection across the *entire* session (a separate unwindowed accumulator, `full_session_frames`, run alongside the existing bounded rolling-window `history` used for the live dashboard - the two serve different purposes and shouldn't share state), compares per-swara actual % against the intended raga's theoretical target %, sorts by biggest deviation, and flags any swara that's both present at >2% and in that raga's `forbidden_notes` list. Printed at session end and included in the JSON summary as `swara_deviation_report`. Wired into both `run_live_mode` (new accumulator) and `run_file_mode` (its existing `history` was already unwindowed, just needed the report computed from it). Tested end-to-end with a synthetic Bhupali performance deliberately contaminated with forbidden Ma_tivra/Ni_shuddha notes: the report correctly surfaced both as the top two deviations with the forbidden-note flag, and - a nice unplanned confirmation of Phase 3's decision to remove the forbidden-note penalty - the classifier independently drifted toward Yaman (which legitimately contains those notes) purely from cosine similarity, no explicit penalty needed. Re-ran `eval_harness.py`: unaffected (180/189), since this is purely additive reporting, not a classifier change. **Not the whole of Phase 7** - still open: cents-level (sub-swara) deviation resolution, and aroha/avroha sequence adherence (needs Phase 8).
- 2026-07-08 — Third real-world failure, root-caused and mitigated: Arun ran a live session against a YouTube Jaunpuri performance and got a FAILED result with `input_tonic_sa_hz: 773.68` - a value far outside any plausible vocal tonic, and noticed the live dashboard was reading "Yaman" during what he identified as the instrumental portion of the track. Root cause: `auto_detect_tonic()` takes the median pitch over whatever audio arrives in the fixed 5-second calibration window after the user hits Enter - it has no way to verify that window actually captured a held Sa rather than an instrumental intro, tanpura/tabla, or anything else that happened to be playing at that moment. Once a wrong tonic like that is set, every downstream cents-relative-to-tonic calculation for the full 120s session is built on it, so the whole histogram is systematically scrambled - consistent with different portions of a scrambled distribution coincidentally resembling different ragas' shapes. This is a distinct, more severe failure mode than the ~30-cent precision limit found earlier in Phase 5 (that was "close but slightly off"; this is "a completely different note, not even the right octave neighborhood"). Mitigated (can't be fully eliminated without much bigger surgery - see below) by adding `confirm_or_override_tonic()`: after auto-detection, the value is shown to the user with a soft plausibility warning if it falls outside a typical vocal tonic range (100-500 Hz, `TYPICAL_TONIC_RANGE_HZ`), and the user can accept it (Enter), override it with a manual number, or in live mode, retry the 5-second capture (`r`) - so an obviously-wrong value like 773.68 Hz gets caught before it silently corrupts a whole session, rather than after. File mode gets the warning/override but not retry, since re-reading the same opening slice of a static file would just return the identical value. Tested the confirmation/override/retry logic directly with simulated inputs (accept, override, retry-then-accept, invalid-input-then-retry) - all paths work correctly. Re-ran `eval_harness.py`: unaffected (180/189), and file-mode end-to-end smoke test still passes. **Not a complete fix** - it only catches out-of-range values; a wrong tonic that happens to land inside 100-500 Hz would sail through undetected. The deeper fix (only start the calibration clock once actual sustained pitched audio is detected, rather than a fixed timer from keypress) is a real algorithmic improvement, not just a UX safety net, and is still open - flagging as a candidate for a future session rather than doing it now.
- 2026-07-08 — Second real-world bug found and fixed: Arun noticed the live dashboard's "Top 3 Nearest Vector Classifications" froze early (100% Marwa, 90%+ Bhairav) and barely moved for the rest of a 120s session. Root cause: `feature_extraction.FeatureExtractor.histogram` only ever accumulates (`self.histogram[bin_index] += 1`, never reset or decayed) - `main_22.py`'s live loop was calling `add_to_histogram()`/`get_normalized_histogram()` per detection and appending those near-duplicate, ever-growing cumulative snapshots into a `history` deque, so the displayed spectrum reflected "everything sung since the process started," converging early and becoming less responsive to new singing the longer the session ran. `config.py` defines `ROLLING_WINDOW_SECONDS = 45.0` and `DECAY_FACTOR = 0.98` suggesting a real rolling/decaying window was intended, but `DECAY_FACTOR` was imported and never actually used anywhere - the windowing was never wired up correctly. Fixed by adding `pitch_to_frame_histogram()`, which builds a one-hot 120-bin frame per individual pitch detection (bypassing the persistent, never-reset `extractor.histogram` entirely) and appending that into the existing bounded deque - `ROLLING_WINDOW_SECONDS` now does what its name says. Removed the dead `DECAY_FACTOR` import. Applied to both live mode (bounded deque, genuine last-~45s window) and file mode (unbounded list is correct there - a one-shot file analysis should reflect the whole file, not a recency window). Verified with a direct test using real synthesized audio through actual pitch detection (not shortcuts): filling the rolling window entirely with Marwa audio classifies as Marwa (1.0), and after enough Bhairav audio fully evicts the old Marwa detections from the bounded window, it correctly re-classifies as Bhairav (1.0) - confirms the display now genuinely tracks what's currently being sung rather than getting stuck. Re-ran `eval_harness.py` (unaffected - it uses its own independent one-shot pipeline per test case, not `main_22.py`'s live loop): still 180/189. This is the second silent-failure-class bug found only by actually running the tool with a real person, following the tonic-parsing bug earlier the same day - neither would have been caught by code review or the eval harness alone, since both are about *live session dynamics* the synthetic one-shot eval harness doesn't exercise.
- 2026-07-08 — Real-world bug found on Arun's first live run of Phase 5's new tonic prompt: typed "auto-detect" at "press Enter to auto-detect", which was truthy (only exactly-blank input triggered the intended path), so `float("auto-detect")` raised, and it silently fell back to the hardcoded `FALLBACK_TONIC_HZ` (145.0) with **no warning** — real auto-detection never ran, but the run still happened to classify correctly (Marwa, PASSED), which is what surfaced the ambiguity in review rather than the bug itself being visible. Fixed: `parse_tonic_input()` now treats *any* unparseable input (blank or not) as "auto-detect," not just blank, and prints `'<input>' isn't a number - auto-detecting tonic instead.` so the fallback is never silent. Re-ran eval_harness.py: unaffected (180/189), since this only touches input parsing, not classifier logic. Lesson: this is the second silent-failure bug found only by actually running the tool with a real person typing real things, not by code review — the file-mode calibration-segment bug in Phase 5 was the first. Worth treating "what if the user types something reasonable-sounding that isn't exactly what I expected" as a first-class test case going forward, not just happy-path input.
- 2026-07-08 — Phase 5 done. Wired up `FeatureExtractor.auto_detect_tonic()` in `main_22.py`: the tonic prompt now accepts a blank answer to trigger auto-detection (median method) instead of requiring a typed Hz value. Added file-based input as an alternative to live mic (`run_file_mode()`, using `librosa.load()` — already a project dependency, no new one added) — you can now point the tool at an audio file instead of only listening live, closing the gap called out in `PROJECT_PLAN.md` section 1 ("or could take a dropped-in music file"). File mode processes the whole file once and prints a one-shot summary instead of the live 120s dashboard loop, since a file has a known finite length. Found and fixed a real bug during testing (not just a theoretical review): when auto-detecting tonic from a file, the opening calibration segment (e.g. a held Sa) was being folded into the *same* histogram used for classification, so it could dominate the result — verified with a synthetic test file (5s held Sa + Yaman aroha/avroha) where this caused the Sa segment to consume 89% of the histogram and misclassify as Puriya. Fixed by excluding the calibration window from the classification chunks when auto-detecting (mirrors how live mode already discarded its calibration chunks correctly). After the fix, tested against clean and short/silent synthetic files — no crashes, and manual-tonic mode with a clean performance file classifies correctly (100% Yaman). One honest residual: auto-detected tonic on a synthetic test landed ~30 cents off the true tonic (223.86 Hz vs. 220 Hz), which alone was enough to push a borderline case to a different raga (Puriya instead of Yaman) — this is a precision ceiling of the existing autocorrelation/median-based `auto_detect_tonic()` algorithm, not something this phase was scoped to fix (Phase 5 = wire it up, not redesign it); manually-entered tonic remains more reliable. Re-ran `eval_harness.py` after all changes: still 180/189 (95.2%), no regression (expected, since `RaagaClassifier.classify()` itself wasn't touched). Rewrote `CLAUDE.md`'s Running/Architecture sections to describe the new prompts and the auto-detect precision caveat.
- 2026-07-08 — Phase 4 done. Deleted `main.py` (its 2-raga registry was a strict subset of `main_22.py`'s, and its one unique feature, the tie-break lock, was already found to measure zero benefit and removed in Phase 3). `main_22.py` is now the sole entry point. Rewrote `CLAUDE.md` end to end — it was still describing the pre-Phase-1 three-database, two-entry-point state, which was actively misleading for any future session. Updated `PROJECT_PLAN.md` section 2 to strike through the now-resolved problems (drifted databases, ad hoc hacks, two entry points) rather than rewriting history. Re-ran `eval_harness.py` after the deletion: still 180/189 (95.2%), no regression. Also resolved a discrepancy the user had introduced directly into this plan file (two Phase-3-tagged design notes — a two-stage forbidden-note elimination filter, and honest-tie reporting for near-identical raga pairs — that I hadn't seen or built): re-measured cosine similarity between raga reference histograms and found the specific 1.0000 ties those notes were written against no longer exist post the Phase 2 weighted-histogram fix (Asavari/Jaunpuri now 0.9365, Marwa/Puriya no longer even near-tied). User decided to defer both ideas to Phase 8 rather than build interim scaffolding for a mostly-resolved problem.
- 2026-07-08 — Phase 3 done. Ablation-tested all three hardcoded hacks (`main.py`'s Yaman similarity bonus, its 90-second Bhupali lock, `main_22.py`'s forbidden-note penalty) against the eval harness — all three measured zero accuracy benefit once Phase 2's weighted histograms were in place, so all three were removed. Re-ran the eval harness after removing them: still 180/189 (95.2%), confirming no regression. Full ablation numbers are in section 6 above.
- 2026-07-08 — Migrated project from Windsurf to Claude Code. Fixed a stray git repo rooted at the home directory (was tracking unrelated personal files/SSH keys); re-initialized git scoped to the project folder, added `.gitignore`, set up SSH auth, pushed initial commit to `github.com/arunathavale/RagaDetector`. Reviewed the codebase and wrote this plan. No code changes yet — Phase 1 (database consolidation) is the next actual coding step.
- 2026-07-08 — Phase 2 started. (1) Built `eval_harness.py`: synthesizes sine-tone renditions of each raga's aroha/avroha/pakad from `config.RAAGA_DATABASE`, runs them through the real pitch-extraction pipeline (no mic needed), and scores `main_22.py`'s classifier. (2) Decided on a real-recording source: the Saraga Hindustani dataset (MTG, CC-BY-NC-SA 4.0) — confirmed by inspecting its GitHub file listing that it covers 6 of 7 target ragas (Yaman, Bhairav, Bhupali, Marwa, Puriya, Asavari as "Komal Rishav Aasavari" — variant name, needs verification) but has no Jaunpuri recordings. Considered `compmusic_raga` (30-raga Hindustani benchmark, CC-BY 4.0) as an alternative/supplement but its audio is gated behind a Zenodo access request. Downloading Saraga audio requires a free personal API token from dunya.compmusic.upf.edu (account signup only the user can do) plus post-download segmentation, since recordings are full multi-raga concerts, not pre-cut labeled clips. **User decided to hold off on real audio and keep iterating on the synthetic harness instead.**
- 2026-07-08 — Tried Option A (cheap fix) on the Jaunpuri/Puriya ceiling: replaced `config.py`'s flat `create_swara_histogram` (which just marked "present/absent") with `compute_swara_weights` + `create_weighted_swara_histogram`, weighting each swara by how often it occurs across a raga's own aroha+avroha+pakad, with a vadi/samvadi emphasis boost. Histograms are now built in a post-construction pass over `RAAGA_DATABASE` instead of inline per-entry, so weights derive from each raga's own data instead of being hand-typed. Result: **eval_harness.py jumped from 71.4% to 95.2% (180/189)** — Jaunpuri and Puriya both went from 0% to 100%. One residual gap: Asavari's avroha-only rendition still misclassifies as Jaunpuri 9/9 times, because Asavari's and Jaunpuri's `avroha` lists in `config.py` were entered as literally identical swara sequences — a data-authoring artifact (these two ragas are genuinely close relatives, same thaat), not a new classifier bug. Worth revisiting if/when the aroha/avroha data gets a more careful musicological pass. Phase 8 (sequence-order matching) is no longer an urgent blocker for these two ragas specifically, though it remains the more correct long-term fix per the original plan.
- 2026-07-08 — Strengthened `eval_harness.py`: instead of one deterministic pure-tone pass, each (raga, sequence type) is now rendered across 3 tonics × 3 trials with random per-note pitch jitter (±15 cents, simulating vocal imprecision) — 189 total cases — plus a confusion matrix. Result: 135/189 (71.4%), and the failure mode is now proven structural rather than a fluke: Jaunpuri → misclassified as Asavari 27/27 times, Puriya → misclassified as Marwa 27/27 times, with zero exceptions across every tonic/jitter combination. These pairs share literally the same `required_notes` set in `config.RAAGA_DATABASE`, so the order-blind flat histogram cannot distinguish them at all — this is a ceiling on accuracy, not classifier mistuning, until Phase 8 (sequence/aroha-avroha-aware matching) or Phase 3 (richer per-raga histogram weighting) is tackled. Everything else (Yaman, Bhupali, Bhairav, Asavari, Marwa) is 100% robust to jitter and tonic shift. **Side note:** an earlier `pip install mirdata` attempt (while researching real-recording sources) partially upgraded numpy/scipy/scikit-learn/librosa in the base conda environment before failing partway through, which silently broke this project's pinned dependencies. Caught it when `eval_harness.py` started throwing `numpy has no attribute 'long'` from librosa/numba; reinstalled the exact versions pinned in `requirements.txt` to fix it. Worth remembering if anything in this environment seems to unexpectedly break: check `pip show` against `requirements.txt` first.
- 2026-07-08 — Phase 1 done. Expanded `config.py::RAAGA_DATABASE` from 2 to all 7 ragas (`Bhairav`, `Asavari`, `Jaunpuri`, `Marwa`, `Puriya` added, each with aroha/avroha/vadi/samvadi/pakad/prahar/forbidden_notes/required_notes/characteristic_phrases). Deleted the inline `RAGA_REGISTRY` + `generate_idealized_histogram()` from both `main.py` and `main_22.py`; both now import `RAAGA_DATABASE` from `config.py` (`main.py` narrows it to `{Yaman, Bhupali}`, matching its original 2-raga scope). `main_22.py`'s forbidden-note penalty check now derives semitone indices from `config.SWARA_MAPPING` instead of hardcoded index lists. Smoke-tested both classifiers against the shared database without audio hardware — imports clean, `config.py`'s own `validate_config()` passes, `classify()`/`check_tie()` run without error on synthetic histograms. Not yet run against live mic input. Next: Phase 2 (eval harness) before touching any classifier logic again.
- 2026-07-10 — Redesigned live-mode tonic handling per Arun's "middle ground" proposal, after he questioned whether a live dashboard built on a manually-typed or blind-5-second-detected tonic was meaningful at all, given both had proven wrong repeatedly this session. `run_live_mode()` no longer prompts for a tonic and no longer has a separate "sing your Sa" window; instead it runs two phases against the same fixed 4-minute `SESSION_DURATION_SECONDS` budget: Phase 1 (`LIVE_TONIC_WINDOW_SECONDS`, 2 min) listens silently — no dashboard — while the singer performs normally, accumulating raw pitches; at the end, the new `determine_tonic_from_pitches()` finds the tonic that makes the already-known intended raga classify most confidently (ranked by gap over 2nd place), by circular-shifting a pitch-class histogram built from those pitches — same technique as `find_optimal_tonic.py`'s `find_best_tonic()`, reimplemented locally since importing it back would be circular. Phase 2 (remaining 2 min) is the normal live dashboard using that calculated tonic. Phase 1's pitches are folded into the session's classification history/deviation report once tonic is known, rather than discarded as calibration-only content the way the old window was. Validated by running `determine_tonic_from_pitches()` against a saved Bhairav recording (`recordings/Bhairav_20260709_211312.wav`) and cross-checking against `find_optimal_tonic.py`'s independent search on the same file: both landed on 135.61 Hz with matching ~50% confidence gaps. `eval_harness.py` re-run for regression safety (95.2%, unchanged — this redesign doesn't touch classifier logic). File mode is unchanged (still prompts for manual/auto-detect tonic, since a file's whole content is available upfront and doesn't have the live-dashboard timing problem this redesign addresses).
- 2026-07-10 — Fixed a fundamental bug in the just-shipped live-mode tonic redesign, caught by Arun: determine_tonic_from_pitches() searched for "the tonic that makes the intended raga classify best," but the intended-raga input is only ever meant to label/save the recording and score results after the fact — the ultimate goal is raga IDENTIFICATION, where the raga is exactly what is unknown, so feeding it into tonic detection is circular (it only validated correctly earlier because the true answer happened to be supplied as the label). Fixed by replacing the targeted classifier search with the raga-agnostic drone-interval prior method already built and validated in find_optimal_tonic.py this session (score_drone_priors()/find_best_tonic_by_drone_priors()) — scores candidate tonics by Sa-Pa/Sa-Ma drone dyad strength, a structural signal from how the tanpura is tuned, independent of what is being sung. Reimplemented locally in main_22.py (importing back from find_optimal_tonic.py would be circular). Also added print_tonic_stability_check(), a local reimplementation of check_tonic_stability(), splitting Phase 1's pitches into 4 segments and flagging disagreement. Validated against the same saved Bhairav recording used to validate the reverted version: drone-prior tonic landed at 141.21 Hz with a 20-cent stability spread, matching find_optimal_tonic.py's independent --drone/stability-check output from earlier this session almost exactly. eval_harness.py re-run for regression safety (95.2%, unchanged). Clarified the raga prompt in main() and CLAUDE.md to state explicitly the label is never given to the detector. Honest caveat: on the highest-confidence ground-truth file, drone-prior scoring landed ~1-2 semitones off the classifier-validated answer — clearly better than a blind guess, not fully solved; the stability check exists to surface that uncertainty rather than hide it.
- 2026-07-10 — Re-analyzed all 10 saved recordings through the new live-mode pipeline (built analyze_recordings.py: 2 min tonic-window, then raga ID + swar deviations, tabulated) per Arun's request instead of continuing single-raga testing. Surfaced two suspicious patterns: 6/10 unrelated recordings converged on the identical 141.21 Hz tonic, and several showed enormous within-recording stability spreads (up to 1150 cents). Root cause: _pitch_autocorrelation() picked the FIRST peak scanning outward from lag 0 with no confidence check - on real audio, that is very often a harmonic/noise spike at a short lag (high, implausible frequency), while the true fundamental sat further out, unexamined. Verified: 64% of pitches in one file were implausible (>600Hz), and roughly half of those had a taller, more plausible peak that was skipped. Fixed by restricting the search to the valid frequency range and picking the tallest peak in range, plus a minimum-confidence (30% of zero-lag) threshold. This dropped eval_harness.py from 95.2% to 74.6%, which took real work to run down: an initial version of the fix also "corrected" an off-by-one in the peak-index math (mathematically defensible in isolation - the derivative zero-crossing marks the true peak at index i+1, not i) which shifts every detection by one lag sample; that alone reproduced the entire regression, concentrated in the Marwa/Puriya pair (same thaat, distinguished only by fine-grained weighting, evidently right on the edge). First chased a wrong hypothesis (eval_harness hard-splices notes together with no crossfade - added, then reverted, a crossfade fix that did not move the number at all) before A/B isolation pinned it on the index correction alone. Reverted just that piece, keeping the actual octave-error fix - restored eval_harness to 180/189 (95.2%) exactly. Also fixed feature_extraction.py's own self-test, which fed an unrealistic full 1-second buffer directly into extract_pitch() (real usage is always ~23ms BUFFER_SIZE chunks) and, once tallest-peak selection was in place, returned 147Hz for a 440Hz tone - a genuine same-pitch harmonic outscoring the fundamental over many cycles, a regime realistic chunk sizes never hit. Net effect on real data: the suspicious 141.21 Hz convergence dropped from 6/10 files to 2/10, and cross-validated closely against yin (an independent pitch method) on one file where they used to disagree substantially. Match rate against the labeled raga is still low (2/10) and stability spreads remain large on most files - a real, verified bug fix, but tonic detection reliability on this real, noisy audio remains a genuinely open problem, not something this fix alone resolves.
